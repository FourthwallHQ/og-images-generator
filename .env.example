add # Google Cloud Platform Configuration
# These environment variables are REQUIRED.

# Storage bucket name for uploading generated OG images
GCP_STORAGE_BUCKET=your-bucket-name

# Pub/Sub topic name for publishing image generation events
GCP_PUBSUB_TOPIC=your-topic-name

# Note: Authentication is handled via Application Default Credentials (ADC)
# - For local development: Run 'gcloud auth application-default login'
# - For Cloud Run: Uses the service account automatically
# - For other environments: Set GOOGLE_APPLICATION_CREDENTIALS to your service account key file path